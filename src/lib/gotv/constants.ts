// Shared GOTV constants — importable from both server and client code.
// Keep in sync with src/lib/operations/metrics-truth.ts (which re-exports calculateWinThreshold).

export const WIN_THRESHOLD_RATIO = 0.35;
