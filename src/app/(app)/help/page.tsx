import HelpCenterClient from "./help-client";

export const metadata = {
  title: "Help Center — Poll City",
  description: "Articles, guides, and answers for running a Canadian municipal campaign on Poll City.",
};

export default function HelpPage() {
  return <HelpCenterClient />;
}
