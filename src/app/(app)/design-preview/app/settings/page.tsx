import { AppLayout } from "@/components/figma-preview/app-layout";
import { Settings } from "@/components/figma-preview/screens/settings";

export default function Page() {
  return (
    <AppLayout>
      <Settings />
    </AppLayout>
  );
}
