import { AppLayout } from "@/components/figma-preview/app-layout";
import { Contacts } from "@/components/figma-preview/screens/contacts";

export default function Page() {
  return (
    <AppLayout>
      <Contacts />
    </AppLayout>
  );
}
