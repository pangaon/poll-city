import { AppLayout } from "@/components/figma-preview/app-layout";
import { WalkList } from "@/components/figma-preview/screens/walk-list";

export default function Page() {
  return (
    <AppLayout>
      <WalkList />
    </AppLayout>
  );
}
