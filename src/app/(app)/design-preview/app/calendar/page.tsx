import { AppLayout } from "@/components/figma-preview/app-layout";
import { Calendar } from "@/components/figma-preview/screens/calendar";

export default function Page() {
  return (
    <AppLayout>
      <Calendar />
    </AppLayout>
  );
}
