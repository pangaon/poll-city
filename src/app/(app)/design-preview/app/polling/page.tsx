import { AppLayout } from "@/components/figma-preview/app-layout";
import { Polling } from "@/components/figma-preview/screens/polling";

export default function Page() {
  return (
    <AppLayout>
      <Polling />
    </AppLayout>
  );
}
