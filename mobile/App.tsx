/**
 * App.tsx — Expo Router entry point.
 *
 * Expo Router uses file-based routing from the `app/` directory.
 * This file simply re-exports the router's entry point as required
 * by the "main": "expo-router/entry" configuration in package.json.
 *
 * All navigation and providers are handled in app/_layout.tsx.
 */

import "expo-router/entry";
